import { Callout, Tabs, Tab } from 'nextra/components'
import TypeDeclarations from 'components/TypeDeclarations'

## 安装

<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tabs.Tab>
    ```bash copy
    pnpm i -D @swc/html
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    npm i -D @swc/html
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    yarn add -D @swc/html
    ```
  </Tabs.Tab>
</Tabs>

## 使用方法

### `minify()` / `minifySync()`

如果你要压缩整个 HTML 文档，可以使用 `minify()` 函数。

```typescript
import { minify } from '@swc/html';

const html = await minify('<div>Hello, world!</div>', {

});
```

### `minifyFragment()` / `minifyFragmentSync()`

如果你要压缩 HTML 片段，可以使用 `minifyFragment()` 函数。

```typescript
import { minifyFragment } from '@swc/html';

const html = await minifyFragment('<div>Hello, world!</div>', {

});
```
